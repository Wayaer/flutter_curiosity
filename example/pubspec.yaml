name: curiosity
description: Demonstrates how to use the curiosity plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  camera: ^0.8.0
  flutter:
    sdk: flutter
  flutter_curiosity:
    path: ../
  # permission_handler: ^5.1.0+2
